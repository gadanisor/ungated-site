[build]
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_OPTIONS = "--max-old-space-size=4096"

# SPA fallback (dacă ai rute care nu sunt fișiere statice)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200